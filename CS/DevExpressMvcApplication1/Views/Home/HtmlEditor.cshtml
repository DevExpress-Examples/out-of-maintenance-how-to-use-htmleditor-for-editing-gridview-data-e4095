@model Employee
@Html.DevExpress().HtmlEditor(
   settings => {
	   settings.Name = "heFeatures";
	   settings.CallbackRouteValues = new { Controller = "Home", Action = "HtmlEditorPartial", employeeID = Model.EmployeeID };
	   settings.Width = 550;
   }).Bind(Model.Notes).GetHtml()
